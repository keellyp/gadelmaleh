{{ include('/partials/header.twig') }}

<div class="container-box">
    <div class="nav-pages">
        <div class="first-box">
            <p class="description">
                Depuis 2000, Gad Elmaleh prête sa voix pour les versions française ou les films d'animation, qui pour lui, est une nouvelle expérience.
            </p>

        </div>
        <div class="container-contents">
            <section>
                <article>
                    <div class="title">
                        <h3>Tous les doublages</h3>
                    </div>
                    <div class="img-box">
                        {% for _dubbing in dubbings %}
                        <div class="poster">
                            <a href="{{ url('dubbing', {id:_dubbing.id}) }}"><img src="{{ global.request.basepath }}/dist/img/dubbing/{{_dubbing.id}}.png" alt="poster"></a>
                        </div>
                        {% endfor %}
                    </div>
                </article>
            </section>
        </div>
    </div>
</div>

{{ include('/partials/footer.twig') }}
